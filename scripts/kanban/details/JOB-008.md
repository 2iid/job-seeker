**Satisfies:** infrastructure.

- [ ] **Given** une pull request **When** la CI tourne **Then** lint, types, tests unitaires, `lint-kanban.sh`, scan de secrets et scan de vulnérabilités s'exécutent
- [ ] **Given** un secret introduit dans le diff **When** la CI tourne **Then** elle échoue et nomme le fichier
- [ ] **Given** le reçu de vérification local **When** la CI tourne **Then** elle **rejoue** le contrat plutôt que de faire confiance au reçu
- [ ] **Given** le budget de minutes d'actions **When** on ajoute un travail **Then** les travaux tournent en parallèle et le cache des dépendances est actif

---

## Étape finale de cette issue — verrouiller la porte sur GitHub

Les protections structurelles de `main` sont déjà en place (PR obligatoire, `enforce_admins`,
historique linéaire, ni force-push ni suppression, conversations résolues). Les **quatre
vérifications requises** ont été volontairement laissées vides : les exiger avant que `vantry.yml`
et l'application existent aurait rendu toute fusion impossible dès le premier jour.

- [ ] **Given** que `JOB-007` a livré un `vantry.yml` dont `scripts/verify.sh` sort `✓ VERIFIED`
      **When** la CI passe au vert sur une PR **Then** les quatre contextes sont ajoutés aux
      vérifications requises de `main`, par leur **nom d'affichage** :
      `contract is valid` · `re-run the verification` · `PR states its evidence` ·
      `sensitive paths need a security review`
- [ ] **Given** les vérifications requises en place **When** on ouvre une PR dont la CI échoue
      **Then** la fusion est refusée — **testé, pas supposé**
- [ ] `strict: true` (la branche doit être à jour avec `main` avant fusion, pour que les
      vérifications aient tourné sur le code qui va réellement atterrir)
- [ ] **Given** `scripts/gen-codeowners.sh` **When** `vantry.yml` déclare ses `sensitive_paths`
      **Then** `.github/CODEOWNERS` est généré et commité (jamais édité à la main)
- [ ] **Deny :** ne jamais activer `require_code_owner_reviews` ni exiger une approbation ≥ 1 tant
      que le dépôt a un seul mainteneur — GitHub interdit d'approuver sa propre PR, et la branche
      deviendrait définitivement infusionnable

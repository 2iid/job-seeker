**Satisfies:** infrastructure — aucune REQ directe.

- [ ] **Given** un dépôt vide **When** on lance l'installation **Then** `apps/web` (Next.js 15, App Router, TypeScript strict) et `apps/worker` (Node + TypeScript) démarrent tous les deux, et `packages/*` est partagé entre eux
- [ ] **Given** le monorepo installé **When** on lance le lint et les tests **Then** les deux commandes existent à la racine et passent sur un projet vide
- [ ] **Given** une modification de type dans `packages/` **When** on compile **Then** l'erreur remonte dans les deux applications — la frontière de types est réelle et non déclarative
- [ ] Le `README` racine dit en trois lignes comment démarrer chacune des deux applications

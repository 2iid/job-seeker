**Satisfies:** infrastructure — sans ce contrat la porte de vérification reste inerte et aucune issue
ne peut être clôturée proprement.

- [ ] **Given** le monorepo **When** on remplit `vantry.yml` **Then** `run.test`, `run.build`, `run.start`, `run.ready` et surtout `run.smoke` sont déclarés et non vides
- [ ] **Given** `run.smoke` **When** on l'exécute **Then** il **exerce le logiciel comme un utilisateur** — au minimum : l'application démarre, une page se rend, une requête aboutit. Une suite de tests unitaires verte ne satisfait pas ce critère
- [ ] **Given** le contrat rempli **When** on lance `scripts/verify.sh` **Then** il imprime `✓ VERIFIED` et écrit un reçu
- [ ] **Given** `strictness` **When** on le règle **Then** il vaut `relaxed` — un projet vide ne peut pas passer une porte stricte au jour 1, et une porte qui bloque tout se fait désinstaller
- [ ] `sensitive_paths` inclut dès maintenant l'authentification, l'envoi sortant, les migrations et le contexte LLM

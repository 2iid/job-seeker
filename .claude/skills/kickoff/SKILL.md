---
name: kickoff
description: Alias for /bootstrap — Vantry's single greenfield inception playbook (idea → CLAUDE.md + docs + team + backlog + sprint plan + board, in one pass). If you typed /kickoff, run the /bootstrap playbook. Trigger words — kickoff, kick off the project.
---
# /kickoff — ≡ /bootstrap

Vantry has **one** greenfield inception playbook: **`/bootstrap`**. It runs the discovery interview and generates the
full agent-native setup — `CLAUDE.md`, the `/docs` suite, ADRs, the **assembled team**, the atomic backlog, the
**sprint plan (Sprint 1 `ACTIVE`)**, and the board — in one guided pass, so `/next` works immediately after.

**→ Run the `/bootstrap` playbook** (with your idea). `/kickoff` exists only so the word still routes you there.

## Done when
`/bootstrap` reported done. Nothing is done *here* — this file only routes the word.

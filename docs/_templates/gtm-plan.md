# Go-to-market plan — <fill: product name>

> Owned by the `growth-strategist` (via `/gtm-plan`). The strategic arc from "shipped" to "users & revenue".
> `docs/growth/growth-log.md` is the execution detail; this is the plan.
>
> **Every `<fill: …>` below must be replaced.** `scripts/check-refs.sh` fails while any survives, and
> `/gtm-plan`'s Done-when requires it to pass — a plan that is this file with the filename changed is not a plan.
> Delete this comment block when the document is real.

## Positioning
For **<fill: ICP>** who **<fill: need>**, **<fill: product>** is the **<fill: category>** that **<fill: unique value>**.

## ICP — the beachhead
<fill: the ONE customer we win first, named concretely — a segment, a size, a trigger. "Everyone" and "SMBs"
are both refusals to choose.>

## Channels (ranked by fit)
Exactly one row carries `✅ first`. Committing to one channel is the point of the ranking.

| # | Channel | Why it fits | Start now? |
|---|---------|-------------|-----------|
| 1 | <fill: e.g. build-in-public on X, or a WhatsApp broadcast, or a local forum> | <fill: reason> | ✅ first |
| 2 | <fill: …> | <fill: reason> | later |

## Pricing & revenue math
- Model: <fill: one-time / subscription / freemium> · Price: <fill: price points>
- To goal: **<fill: price> × <fill: N> customers = <fill: target>**.
- Reconciled with `docs/planning/project-brief.md` `## Unit economics`? <fill: yes, and the price matches / no
  unit economics were written / it differs, because …>

## Launch plan
<fill: the sequence and which assets — generate them with `/launch-kit`>

## 90-day growth plan
| Weeks | Focus | Primary metric |
|-------|-------|----------------|
| 1–2 | launch + <fill: channel> | <fill: metric> |
| 3–6 | activation | <fill: metric> |
| 7–12 | retention + referral | <fill: metric> |

## Metrics (AARRR)
`/growth-review` step 1 reads this table and cannot run without it. Every Instrument names a real tool or a real
event — "analytics" is not an instrument.

| Stage | Metric | Instrument |
|-------|--------|-----------|
| Acquisition | <fill: metric> | <fill: tool or event> |
| Activation | <fill: metric> | <fill: tool or event> |
| Retention | <fill: metric> | <fill: tool or event> |
| Referral | <fill: metric> | <fill: tool or event> |
| Revenue | <fill: metric> | <fill: tool or event> |

## Claims that need a human
Every competitor claim, market-size figure and price benchmark below is asserted by a person, not by a model.
Anything unconfirmed is marked `(assumed — confirm)`.

| Claim | Source | Confirmed by |
|---|---|---|
| <fill: claim> | <fill: where it came from> | <fill: name, or "(assumed — confirm)"> |
